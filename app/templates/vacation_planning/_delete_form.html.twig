<form method="post" action="{{ path('app_vacation_planning_delete', {'id': vacation_planning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vacation_planning.id) }}">
    <button class="btn">Delete</button>
</form>
