{% extends 'base.html.twig' %}

{% block title %}VacationPlanning{% endblock %}

{% block body %}
    <h1>VacationPlanning</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vacation_planning.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ vacation_planning.startDate ? vacation_planning.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ vacation_planning.endDate ? vacation_planning.endDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vacation_planning_index') }}">back to list</a>

    <a href="{{ path('app_vacation_planning_edit', {'id': vacation_planning.id}) }}">edit</a>

    {{ include('vacation_planning/_delete_form.html.twig') }}
{% endblock %}
